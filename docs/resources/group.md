---
page_title: "gravitino_group Resource - terraform-provider-gravitino"
subcategory: ""
description: |-
  
---

# gravitino_group Resource



## Example Usage

```terraform
# Minimal group. The API has no group update endpoint, so changing the name or
# metalake replaces the resource.
resource "gravitino_group" "analysts" {
  metalake = gravitino_metalake.example.name
  name     = "analytics_team"
}

# Group with roles assigned. Roles are granted and revoked in-place through the
# permissions API.
resource "gravitino_group" "engineers" {
  metalake = gravitino_metalake.example.name
  name     = "engineering"
  roles    = ["admin"]
}
```

## Import

Import is supported using the following syntax:

```shell
# Import an existing group by metalake and group name.
terraform import gravitino_group.example my_metalake.my_group
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `metalake` (String) The metalake name. Changing it requires creating a new group.
- `name` (String) The group name. The API has no rename endpoint, so changing it requires creating a new group.

### Optional

- `roles` (Set of String) The roles assigned to the group. Mutated in-place through the permissions grant/revoke endpoints.

### Read-Only

- `audit` (Object) Audit information for the group. (see [below for nested schema](#nestedatt--audit))
- `id` (String) Composite identifier in the format 'metalake.group_name'.

<a id="nestedatt--audit"></a>
### Nested Schema for `audit`

Read-Only:

- `create_time` (String)
- `creator` (String)
- `last_modified_time` (String)
- `last_modifier` (String)
