---
page_title: "gravitino_idp_group Resource - terraform-provider-gravitino"
subcategory: ""
description: |-
  Manages a built-in IDP group for local authentication; the built-in IDP REST API needs gravitino.authenticators = basic (without simple) and gravitino.server.rest.extensionPackages = org.apache.gravitino.idp.web.rest.feature, and calls must come from a gravitino.authorization.serviceAdmins service admin, otherwise the endpoint answers HTTP 404.
---

# gravitino_idp_group Resource

Manages a built-in IDP group for local authentication; the built-in IDP REST API needs `gravitino.authenticators = basic` (without `simple`) and `gravitino.server.rest.extensionPackages = org.apache.gravitino.idp.web.rest.feature`, and calls must come from a `gravitino.authorization.serviceAdmins` service admin, otherwise the endpoint answers HTTP 404.

## Example Usage

```terraform
# Built-in IDP group with members
resource "gravitino_idp_group" "engineers" {
  name  = "engineers"
  users = ["alice", "bob"]
}
```

## Import

Import is supported using the following syntax:

```shell
# Import an existing IDP group by its name.
terraform import gravitino_idp_group.example my_idp_group
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The group name.

### Optional

- `users` (Set of String) The usernames of members in the group.

### Read-Only

- `id` (String) The name of the built-in IDP group.
