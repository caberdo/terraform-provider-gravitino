---
page_title: "gravitino_user Resource - terraform-provider-gravitino"
subcategory: ""
description: |-
  
---

# gravitino_user Resource



## Example Usage

```terraform
# Minimal user. The API has no user update endpoint, so changing the name or
# metalake replaces the resource.
resource "gravitino_user" "analyst" {
  metalake = gravitino_metalake.example.name
  name     = "data_analyst"
}

# User with roles assigned. Roles are granted and revoked in-place through the
# permissions API.
resource "gravitino_user" "engineer" {
  metalake = gravitino_metalake.example.name
  name     = "data_engineer"
  roles    = ["admin", "readonly"]
}
```

## Import

Import is supported using the following syntax:

```shell
# Import an existing user by metalake and user name.
terraform import gravitino_user.example my_metalake.my_user
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `metalake` (String) The metalake name. Changing it requires creating a new user.
- `name` (String) The user name. The API has no rename endpoint, so changing it requires creating a new user.

### Optional

- `roles` (Set of String) The roles assigned to the user. Mutated in-place through the permissions grant/revoke endpoints.

### Read-Only

- `audit` (Object) Audit information for the user. (see [below for nested schema](#nestedatt--audit))
- `id` (String) Composite identifier in the format 'metalake.user_name'.

<a id="nestedatt--audit"></a>
### Nested Schema for `audit`

Read-Only:

- `create_time` (String)
- `creator` (String)
- `last_modified_time` (String)
- `last_modifier` (String)
